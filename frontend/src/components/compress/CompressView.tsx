import React from 'react';
import { Box, Typography } from '@mui/material';

const CompressView: React.FC = () => {
  return (
    <Box>
      <Typography>圧縮ビューコンポーネント（開発中）</Typography>
    </Box>
  );
};

export default CompressView;

export {};
