import React from 'react';
import { Box, Typography } from '@mui/material';

const AlbumsView: React.FC = () => {
  return (
    <Box>
      <Typography>アルバムビューコンポーネント（開発中）</Typography>
    </Box>
  );
};

export default AlbumsView;

export {};
