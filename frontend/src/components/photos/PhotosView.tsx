import React from 'react';
import { Box, Typography } from '@mui/material';

const PhotosView: React.FC = () => {
  return (
    <Box>
      <Typography>写真ビューコンポーネント（開発中）</Typography>
    </Box>
  );
};

export default PhotosView;

export {};
