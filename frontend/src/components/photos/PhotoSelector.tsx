import React from 'react';

const PhotoSelector: React.FC = () => {
  return (
    <div>
      {/* 写真選択コンポーネント - 実装予定 */}
    </div>
  );
};

export default PhotoSelector;

// TypeScriptのモジュールとして認識させるための空のexport文
export {};