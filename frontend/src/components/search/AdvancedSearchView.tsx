import React from 'react';
import { Box, Typography } from '@mui/material';

const AdvancedSearchView: React.FC = () => {
  return (
    <Box>
      <Typography>高度な検索ビューコンポーネント（開発中）</Typography>
    </Box>
  );
};

export default AdvancedSearchView;

export {};
